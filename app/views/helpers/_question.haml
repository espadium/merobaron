.question-wrapper.hero-unit{ class: question.name }
  %h2 
    = question.label
  - question.items.each do |item|
    .question-content
      = haml :'helpers/_item', locals: { item: item, question: question }, layout: false
